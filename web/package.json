{
  "name": "quant-trading-research-console",
  "private": true,
  "version": "0.2.0-pf08",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc -p tsconfig.json && vite build",
    "test:view-models": "rm -rf .test-dist && tsc -p tsconfig.test.json && node --test .test-dist/tests/*.test.js",
    "validate:types": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "vite": "5.4.10",
    "typescript": "5.8.3",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  }
}
